<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Admin Dashboard</title>
  <style>
    /* Root colors for light and dark themes */
    :root {
      --color-bg-light: #f5f7fa;
      --color-bg-dark: #1e1e2f;
      --color-header-bg-light: #ffffff;
      --color-header-bg-dark: #2c2c44;
      --color-sidebar-bg-light: #ffffff;
      --color-sidebar-bg-dark: #292940;
      --color-footer-bg-light: #ffffff;
      --color-footer-bg-dark: #2c2c44;
      --color-text-light: #1a1a1a;
      --color-text-dark: #eaeaea;
      --color-accent: #4a90e2;
      --color-link-hover: #357ABD;
    }

    /* Reset and base */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: var(--color-bg-light);
      color: var(--color-text-light);
      display: grid;
      grid-template-columns: 240px 1fr;
      grid-template-rows: 60px 1fr 40px;
      grid-template-areas:
        "header header"
        "sidebar main"
        "footer footer";
      height: 100vh;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    header {
      grid-area: header;
      background-color: var(--color-header-bg-light);
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 20px;
      box-shadow: 0 1px 5px rgba(0,0,0,0.1);
    }

    header h1 {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--color-accent);
    }

    /* Theme switch container */
    .theme-switch {
      display: flex;
      align-items: center;
      cursor: pointer;
      user-select: none;
    }

    .theme-switch input[type="checkbox"] {
      display: none;
    }

    .slider {
      width: 40px;
      height: 20px;
      background-color: #ccc;
      border-radius: 20px;
      position: relative;
      transition: background-color 0.3s ease;
    }

    .slider::before {
      content: "";
      position: absolute;
      width: 16px;
      height: 16px;
      left: 2px;
      top: 2px;
      background-color: white;
      border-radius: 50%;
      transition: transform 0.3s ease;
    }

    input[type="checkbox"]:checked + .slider {
      background-color: var(--color-accent);
    }

    input[type="checkbox"]:checked + .slider::before {
      transform: translateX(20px);
    }

    /* Sidebar */
    aside {
      grid-area: sidebar;
      background-color: var(--color-sidebar-bg-light);
      padding: 20px;
      box-shadow: inset -1px 0 0 rgba(0,0,0,0.1);
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    aside nav a {
      text-decoration: none;
      color: var(--color-text-light);
      font-weight: 600;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background-color 0.2s ease;
    }

    aside nav a:hover,
    aside nav a:focus {
      background-color: var(--color-accent);
      color: white;
    }

    /* Main content */
    main {
      grid-area: main;
      padding: 30px;
      overflow-y: auto;
    }

    main h2 {
      margin-bottom: 20px;
      font-weight: 700;
      color: var(--color-accent);
    }

    main p {
      font-size: 1.1rem;
      line-height: 1.5;
      max-width: 600px;
    }

    /* Footer */
    footer {
      grid-area: footer;
      background-color: var(--color-footer-bg-light);
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 0.9rem;
      color: var(--color-text-light);
      box-shadow: 0 -1px 5px rgba(0,0,0,0.1);
    }

    /* Dark theme */
    body.dark {
      background-color: var(--color-bg-dark);
      color: var(--color-text-dark);
    }

    body.dark header {
      background-color: var(--color-header-bg-dark);
      box-shadow: 0 1px 5px rgba(0,0,0,0.7);
    }

    body.dark aside {
      background-color: var(--color-sidebar-bg-dark);
      box-shadow: inset -1px 0 0 rgba(255,255,255,0.1);
    }

    body.dark aside nav a {
      color: var(--color-text-dark);
    }

    body.dark aside nav a:hover,
    body.dark aside nav a:focus {
      background-color: var(--color-accent);
      color: white;
    }

    body.dark footer {
      background-color: var(--color-footer-bg-dark);
      color: var(--color-text-dark);
      box-shadow: 0 -1px 5px rgba(0,0,0,0.7);
    }

    body.dark main h2 {
      color: var(--color-accent);
    }

    /* Responsive */
    @media (max-width: 768px) {
      body {
        grid-template-columns: 1fr;
        grid-template-rows: 60px auto 40px 1fr;
        grid-template-areas:
          "header"
          "sidebar"
          "footer"
          "main";
      }

      aside {
        flex-direction: row;
        justify-content: space-around;
        padding: 10px;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Admin Dashboard</h1>
    <label class="theme-switch" aria-label="Toggle light and dark theme">
      <input type="checkbox" id="theme-toggle" />
      <span class="slider"></span>
    </label>
  </header>

  <aside>
    <nav>
      <a href="#" tabindex="0">Home</a>
      <a href="#" tabindex="0">Users</a>
      <a href="#" tabindex="0">Settings</a>
    </nav>
  </aside>

  <main>
    <h2>Welcome to the Dashboard</h2>
    <p>
      This is your main content area where you can add reports, data visualization,
      or other important information for your admin panel.
    </p>
  </main>

  <footer>
    &copy; 2025 Admin Dashboard. All rights reserved.
  </footer>

  <script>
    const toggleSwitch = document.getElementById('theme-toggle');
    toggleSwitch.addEventListener('change', () => {
      document.body.classList.toggle('dark', toggleSwitch.checked);
    });

    // Optional: Persist theme across page reloads (using localStorage)
    // On load, check saved preference
    window.addEventListener('DOMContentLoaded', () => {
      const savedTheme = localStorage.getItem('theme');
      if (savedTheme === 'dark') {
        document.body.classList.add('dark');
        toggleSwitch.checked = true;
      }
    });

    toggleSwitch.addEventListener('change', () => {
      if (toggleSwitch.checked) {
        localStorage.setItem('theme', 'dark');
      } else {
        localStorage.setItem('theme', 'light');
      }
    });
  </script>
</body>
</html>
